# This is a template docker env file 
# Make sure to change the defaults and gitignore is set to ignore .env  

# The postgres database root username and password
PGPUSER=postgres
PGPASSWORD=changeme

# The pgadmin users email address and password
PGAEMAIL=pgadmin@yourdomain.local
PGAPASSWORD=changeme
